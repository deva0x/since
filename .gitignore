# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# since stores snapshots of the local machine under ~/.local/state/since —
# never in the repo. But guard against a stray local state dir here too:
state/
snapshots/
*.snapshot.json

# Private working docs kept LOCAL only (personal governance kit) — never publish.
RULES.md
VPS-security.md
CLAUDE.md
PENDING.md
docs/
v2_kimi_findings.md
v3_kimi_findings.md

# Python build artifacts
build/
dist/
*.egg-info/
